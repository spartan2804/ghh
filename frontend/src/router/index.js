// index.js - Vue Router setup placeholder

import { createRouter, createWebHistory } from 'vue-router';

const routes = [
  // Add your routes here
];

const router = createRouter({
  history: createWebHistory(),
  routes,
});

export default router; 